import { createStore } from 'vuex'
import menuModule from './modules/menu'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    menu: menuModule
  }
})
